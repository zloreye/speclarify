---
name: project-roadmap
description: 规划项目开发路线并生成开发路线图。
---

读取 `specs/项目文档.md`、`specs/技术栈.md` 和 `specs/项目结构.md`，通过 `clarify` 完成开发路线决策。

覆盖项目文档中确认的全部开发范围，在开发路线图中明确区分 MVP 与后续任务，按依赖关系安排顺序，并优先以可验证的用户行为组织垂直切片；开发路线图中应包含项目初始化任务。每个任务使用 `- [ ]` 标记未完成状态。

先生成开发路线图的结构预览；用户确认后，按照该结构生成 `specs/开发路线图.md`。
